const path = require('path');
// const result = require('./model/databases/queries')

exports.get = (req, res) => {
  //{result} needs to be passed in below when query written
  res.render('result');
};
